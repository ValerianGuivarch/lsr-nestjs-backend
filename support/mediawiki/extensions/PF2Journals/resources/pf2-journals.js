( function () { 'use strict'; function page() { return mw.config.get( 'wgPageName' ).replace( /_/g, ' ' ); } function number( value ) { var found = /^Journal-(\d+)$/.exec( value ); return found ? Number( found[1] ) : null; } function link( title, text ) { var node = document.createElement( 'a' ); node.href = mw.util.getUrl( title ); node.textContent = text; return node; } function root() { var content = document.querySelector( '#mw-content-text' ); if ( !content ) { return null; } var node = document.createElement( 'section' ); node.id = 'pf2-journals'; content.textContent = ''; content.appendChild( node ); return node; } function heading( value ) { var node = document.querySelector( '.mw-page-title-main, #firstHeading' ); if ( node ) { node.textContent = value; } } function locked( item, admin ) { var row = document.createElement( 'article' ); row.className = 'pf2-journal-card pf2-journal-locked'; var text = item.number + ' - ' + item.title; if ( admin ) { row.appendChild( link( 'Journal-' + item.number, '🔒 ' + text ) ); var note = document.createElement( 'small' ); note.textContent = 'Non disponible pour le moment'; row.appendChild( note ); } else { row.textContent = '🔒 ' + text; } return row; } function list( payload, node ) { heading( 'Journaux' ); var h = document.createElement( 'h2' ); h.textContent = 'Journaux'; node.appendChild( h ); var grid = document.createElement( 'div' ); grid.className = 'pf2-journal-grid'; payload.data.forEach( function ( item ) { if ( item.revealed ) { var row = document.createElement( 'article' ); row.className = 'pf2-journal-card pf2-journal-revealed'; row.appendChild( link( 'Journal-' + item.number, item.number + ' - ' + item.title ) ); grid.appendChild( row ); } else { grid.appendChild( locked( item, payload.canAdmin ) ); } } ); node.appendChild( grid ); } function detail( payload, node ) { var item = payload.data; heading( 'Journal ' + item.number + ' — ' + item.title ); var h = document.createElement( 'h2' ); h.textContent = item.number + ' - ' + item.title; node.appendChild( h ); if ( !item.revealed ) { if ( !payload.canAdmin || typeof item.content !== 'string' ) { var hidden = document.createElement( 'p' ); hidden.className = 'pf2-journal-hidden'; hidden.textContent = 'Journal non révélé'; node.appendChild( hidden ); return; } var notice = document.createElement( 'p' ); notice.className = 'pf2-journal-admin-notice'; notice.textContent = 'Non disponible pour le moment'; node.appendChild( notice ); } var content = document.createElement( 'div' ); content.className = 'pf2-journal-content'; content.textContent = item.content || ''; node.appendChild( content ); } $( function () { var current = page(); var journal = number( current ); if ( current !== 'Journaux' && journal === null ) { return; } var node = root(); if ( !node ) { return; } new mw.Api().get( { action: 'pf2journals', format: 'json', number: journal || undefined } ).then( function ( response ) { if ( current === 'Journaux' ) { list( response.pf2journals, node ); } else { detail( response.pf2journals, node ); } } ).catch( function () { node.textContent = 'Impossible de charger les journaux.'; } ); } ); }() );
