# jdr-domain

Canonical business domain models for the generic JdR system.

This library contains only pure domain code:

- entities and value objects;
- enums and domain types;
- invariant checks and stat computation;
- slug normalization and collision handling helpers.

No NestJS, HTTP DTO or persistence code belongs here.