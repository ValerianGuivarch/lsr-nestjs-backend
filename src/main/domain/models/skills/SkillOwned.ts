/*export class SkillOwned {
  id: number
  skill: Skill
  use: SkillOwnedUse
  limitedUse: number
  pvCost: number
  pfCost: number
  ppCost: number
  dettesCost: number
  arcaneCost: number

  constructor(p: SkillOwned) {
    this.id = p.id
    this.skill = p.skill
    this.use = p.use
    this.pvCost = p.pvCost
    this.pfCost = p.pfCost
    this.ppCost = p.ppCost
    this.dettesCost = p.dettesCost
    this.arcaneCost = p.arcaneCost
  }
}
*/
